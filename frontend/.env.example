# Compass frontend - environment configuration.
# Copy to .env.local for local dev (never commit .env.local - see root .gitignore).

# --- Zero-AWS demo/dev path -------------------------------------------------
# USE_MOCK=true short-circuits lib/api.ts to lib/mock/* fixtures for every
# route in docs/CONTRACTS.md - the whole app renders with no AWS resources
# deployed. AUTH_DISABLED=true skips the Cognito Hosted UI redirect and
# synthesizes a persona (switchable via the sidebar persona switcher) instead.
# The two are independent flags but are meant to be used together for the
# offline/dev path; set both to "false" for a real deployed environment.
NEXT_PUBLIC_USE_MOCK=true
NEXT_PUBLIC_AUTH_DISABLED=true

# --- Backend API (HttpApi custom domain or execute-api invoke URL) ---------
# e.g. https://api.compass-demo.example.com or the API Gateway invoke URL for
# the deployed stage. Leave blank while NEXT_PUBLIC_USE_MOCK=true.
NEXT_PUBLIC_API_BASE_URL=

# --- Cognito (ignored while NEXT_PUBLIC_AUTH_DISABLED=true) ----------------
# User pool issuer, e.g. https://cognito-idp.us-east-1.amazonaws.com/us-east-1_xxxxxxxxx
NEXT_PUBLIC_COGNITO_AUTHORITY=
# Hosted UI domain, e.g. https://compass-demo.auth.us-east-1.amazoncognito.com
NEXT_PUBLIC_COGNITO_DOMAIN=
NEXT_PUBLIC_COGNITO_CLIENT_ID=
NEXT_PUBLIC_COGNITO_REDIRECT_URI=http://localhost:3000/login/
NEXT_PUBLIC_COGNITO_POST_LOGOUT_REDIRECT_URI=http://localhost:3000/login/
# required enables TOTP. team_preview keeps named users and passwords but bypasses TOTP.
NEXT_PUBLIC_MFA_MODE=required
