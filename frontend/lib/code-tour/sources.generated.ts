// GENERATED by scripts/generate-code-tour.mjs - do not edit by hand.
// Source revision: 46612ed
export type CodeTourFile = {
  file: string; title: string; note: string;
  startLine: number; endLine: number; totalLines: number; code: string;
};
export const CODE_TOUR_REVISION = "46612ed";
export const CODE_TOUR: Record<string, CodeTourFile[]> = {
  "element-1": [
    {
      "file": "src/functions/authorizer/app.py",
      "title": "Independent token verifier",
      "note": "Re-validates every JWT (signature, issuer, expiry) and derives role and org unit. Zero trust: verified twice, never assumed.",
      "startLine": 1,
      "endLine": 287,
      "totalLines": 287,
      "code": "\"\"\"Compass identity: the Cognito JWT context authorizer + ``GET /me``.\n\nOne Lambda, two jobs (both driven by the same claims → role → org_unit map):\n\n1. **Lambda REQUEST authorizer** (registered in template.yaml as\n   ``CompassContextAuthorizer``, payload format 2.0, simple responses). It\n   validates the Cognito JWT *itself* - signature against the user pool's\n   JWKS, issuer, expiry, and audience - then derives the caller's ``role``\n   from ``cognito:groups`` and the ``org_unit`` that Postgres row-level\n   security keys on, and injects both into the request context. Anything it\n   cannot fully verify is denied: ``{\"isAuthorized\": false}``. Deny-by-default,\n   no exceptions.\n\n2. **``GET /me``** (element 1). That route sits behind the API's *default*\n   authorizer (the native HttpApi JWT authorizer), so the token is already\n   verified by API Gateway and the raw Cognito claims arrive on\n   ``requestContext.authorizer.jwt.claims``. The handler applies the same\n   group→role→org_unit mapping and returns the identity the SPA renders.\n\nThe mapping (docs/CONTRACTS.md \"RLS\", and the two ``AWS::Cognito::UserPoolGroup``\nresources in template.yaml)::\n\n    cognito group        role         org_unit         sees\n    -------------------  -----------  ---------------  ---------------------------\n    compass-poweruser    poweruser    ONR-Corporate    every row (corporate branch)\n    compass-viewer       viewer       Code-30          only Code-30 rows\n\n``org_unit`` is what the request-scoped ``SET LOCAL compass.org_unit`` in\n``compass_common.db.set_org`` binds, which is what the ``grants_curated``\npolicies read. So this file is where \"which JWT group you are in\" becomes\n\"which rows the database will hand you\" - it is the front door of the whole\naccess-control story.\n\nEnvironment\n-----------\nUSER_POOL_ID   Cognito user pool id (required).\nWEB_CLIENT_ID  App client id; checked against ``aud`` (ID tokens) or\n               ``client_id`` (access tokens).\nAWS_REGION     Supplied by the Lambda runtime; used to build the issuer URL.\nJWKS_CACHE_SECONDS  Signing-key cache lifetime.            default 3600\n\nDependencies: PyJWT + cryptography, from this function's own\n``requirements.txt`` (deliberately not in the shared CommonLayer - only this\nfunction verifies tokens). This function has **no VpcConfig** in the template,\nso it can reach the public Cognito JWKS endpoint.\n\"\"\"\nfrom __future__ import annotations\n\nimport os\nfrom typing import Any, Dict, List, Optional\n\nfrom compass_common import http\n\n# --------------------------------------------------------------------------- #\n# Compatibility aliases point to the shared identity contract. The shared\n# module is the only source of truth used by authorizers and route handlers.\n# --------------------------------------------------------------------------- #\nGROUP_ROLE = http.GROUP_TO_ROLE\nROLE_ORG_UNIT = http.ROLE_TO_ORG_UNIT\nROLE_PRECEDENCE = http.ROLE_PRECEDENCE\n\n_JWK_CLIENT = None\n\n\nclass AuthError(Exception):\n    \"\"\"Token could not be verified - always answered with a deny.\"\"\"\n\n\n# --------------------------------------------------------------------------- #\n# Claims → role → org_unit\n# --------------------------------------------------------------------------- #\ndef role_from_groups(groups: List[str]) -> Optional[str]:\n    \"\"\"Highest-precedence Compass role in ``groups``; ``None`` if none apply.\"\"\"\n    return http.role_from_groups(groups)\n\n\ndef org_unit_for_role(role: Optional[str]) -> Optional[str]:\n    return http.org_unit_for_role(role)\n\n\n# --------------------------------------------------------------------------- #\n# Token verification\n# --------------------------------------------------------------------------- #\ndef _region() -> str:\n    return (\n        os.environ.get(\"AWS_REGION\")\n        or os.environ.get(\"AWS_DEFAULT_REGION\")\n        or \"us-east-1\"\n    )\n\n\ndef issuer() -> str:\n    pool = os.environ.get(\"USER_POOL_ID\")\n    if not pool:\n        raise AuthError(\"USER_POOL_ID is not configured\")\n    return f\"https://cognito-idp.{_region()}.amazonaws.com/{pool}\"\n\n\ndef jwks_url() -> str:\n    return f\"{issuer()}/.well-known/jwks.json\"\n\n\ndef _jwk_client():\n    \"\"\"Module-level cached ``PyJWKClient`` (keys cached across warm invokes).\"\"\"\n    global _JWK_CLIENT\n    if _JWK_CLIENT is None:\n        from jwt import PyJWKClient\n\n        _JWK_CLIENT = PyJWKClient(\n            jwks_url(),\n            cache_keys=True,\n            lifespan=int(os.environ.get(\"JWKS_CACHE_SECONDS\", \"3600\")),\n        )\n    return _JWK_CLIENT\n\n\ndef bearer_token(event: Dict[str, Any]) -> Optional[str]:\n    \"\"\"Pull the raw JWT out of an authorizer or HTTP API event.\"\"\"\n    candidates: List[str] = []\n    ident = event.get(\"identitySource\")\n    if isinstance(ident, list):\n        candidates.extend(str(v) for v in ident if v)\n    headers = {str(k).lower(): v for k, v in (event.get(\"headers\") or {}).items()}\n    for name in (\"authorization\", \"x-amz-security-token\"):\n        if headers.get(name):\n            candidates.append(str(headers[name]))\n    for raw in candidates:\n        token = raw.strip()\n        if token.lower().startswith(\"bearer \"):\n            token = token[7:].strip()\n        if token:\n            return token\n    return None\n\n\ndef verify_token(token: str, *, jwk_client=None) -> Dict[str, Any]:\n    \"\"\"Verify a Cognito JWT and return its claims, or raise :class:`AuthError`.\n\n    Checks signature (RS256, against the pool's JWKS), issuer, ``exp``/``nbf``\n    (60s leeway for clock skew), and the audience - Cognito puts the app client\n    id in ``aud`` on ID tokens and in ``client_id`` on access tokens, so the\n    check follows ``token_use``. Both token types are accepted: the SPA sends\n    ID tokens, and machine callers/curl typically send access tokens.\n\n    ``jwk_client`` is injectable so this can be exercised offline against a\n    fake key provider.\n    \"\"\"\n    import jwt  # PyJWT - from this function's requirements.txt\n\n    if not token:\n        raise AuthError(\"missing bearer token\")\n    client = jwk_client or _jwk_client()\n    try:\n        signing_key = client.get_signing_key_from_jwt(token)\n    except Exception as exc:  # unknown kid, unreachable/invalid JWKS, malformed\n        raise AuthError(f\"cannot resolve signing key: {exc}\") from exc\n\n    try:\n        claims = jwt.decode(\n            token,\n            signing_key.key,\n            algorithms=[\"RS256\"],\n            issuer=issuer(),\n            leeway=60,\n            # Audience lives in different claims per token_use; checked below.\n            options={\"verify_aud\": False, \"require\": [\"exp\", \"iss\", \"sub\"]},\n        )\n    except Exception as exc:\n        raise AuthError(f\"token rejected: {exc}\") from exc\n\n    expected_client = os.environ.get(\"WEB_CLIENT_ID\")\n    token_use = claims.get(\"token_use\")\n    if expected_client:\n        if token_use == \"id\":\n            aud = claims.get(\"aud\")\n            aud_list = aud if isinstance(aud, list) else [aud]\n            if expected_client not in aud_list:\n                raise AuthError(\"id token audience does not match the app client\")\n        elif token_use == \"access\":\n            if claims.get(\"client_id\") != expected_client:\n                raise AuthError(\"access token client_id does not match the app client\")\n        else:\n            raise AuthError(f\"unsupported token_use {token_use!r}\")\n    return claims\n\n\ndef _groups(claims: Dict[str, Any]) -> List[str]:\n    return http._split_groups(claims.get(\"cognito:groups\"))\n\n\n# --------------------------------------------------------------------------- #\n# 1) Lambda REQUEST authorizer (simple response format)\n# --------------------------------------------------------------------------- #\ndef _deny(reason: str) -> Dict[str, Any]:\n    print(f'{{\"event_type\":\"authz_deny\",\"reason\":\"{reason}\"}}')\n    return {\"isAuthorized\": False, \"context\": {\"reason\": reason}}\n\n\ndef authorize(event: Dict[str, Any]) -> Dict[str, Any]:\n    \"\"\"Verify the token and return the authz context (or a deny).\n\n    On allow, ``context`` carries the values every downstream handler reads via\n    ``compass_common.http.get_claims``: ``sub``, ``username``, ``email``,\n    ``role``, ``org_unit``, ``groups``. API Gateway stringifies context values,\n    so ``groups`` is emitted comma-joined (``get_claims`` splits it back).\n    \"\"\"\n    try:\n        claims = verify_token(bearer_token(event) or \"\")\n    except AuthError as exc:\n        return _deny(str(exc))\n\n    groups = _groups(claims)\n    role = role_from_groups(groups)\n    if role is None:\n        return _deny(\"no Compass group on the token (expected compass-poweruser or compass-viewer)\")\n    org_unit = org_unit_for_role(role)\n    if not org_unit:\n        return _deny(f\"no org_unit mapped for role {role!r}\")\n\n    return {\n        \"isAuthorized\": True,\n        \"context\": {\n            \"sub\": str(claims.get(\"sub\", \"\")),\n            \"username\": str(\n                claims.get(\"cognito:username\") or claims.get(\"username\") or \"\"\n            ),\n            \"email\": str(claims.get(\"email\", \"\")),\n            \"role\": role,\n            \"org_unit\": org_unit,\n            \"groups\": \",\".join(groups),\n        },\n    }\n\n\n# --------------------------------------------------------------------------- #\n# 2) GET /me\n# --------------------------------------------------------------------------- #\ndef _display_name(raw: Dict[str, Any], email: Optional[str], role: str) -> str:\n    for key in (\"name\", \"given_name\", \"preferred_username\"):\n        val = raw.get(key)\n        if val:\n            return str(val)\n    if email:\n        return str(email).split(\"@\")[0]\n    if raw.get(\"cognito:username\"):\n        return str(raw[\"cognito:username\"])\n    return \"Power User\" if role == \"poweruser\" else \"Viewer\"\n\n\ndef get_me(event: Dict[str, Any]) -> Dict[str, Any]:\n    \"\"\"``GET /me`` → the caller's identity, role and RLS org_unit.\n\n    Works behind either authorizer: ``get_claims`` reads the Lambda\n    authorizer's injected context when present, and the native JWT\n    authorizer's raw Cognito claims otherwise (in which case role/org_unit are\n    derived here from ``cognito:groups``).\n    \"\"\"\n    claims = http.get_claims(event)\n    role, org_unit = http.resolve_identity(claims)\n    if role not in ROLE_ORG_UNIT:\n        return http.forbidden(\n            \"caller is not in a Compass group (compass-poweruser or compass-viewer)\"\n        )\n    return http.ok(\n        {\n            \"sub\": claims.sub or \"\",\n            \"email\": claims.email or \"\",\n            \"display_name\": _display_name(claims.raw, claims.email, role),\n            \"role\": role,\n            \"org_unit\": org_unit,\n            \"groups\": claims.groups,\n        }\n    )\n\n\n# --------------------------------------------------------------------------- #\n# Entry point\n# --------------------------------------------------------------------------- #\ndef handler(event, context=None):\n    \"\"\"Route by event shape: authorizer invocation vs. HTTP API request.\"\"\"\n    event = event or {}\n    if event.get(\"type\") == \"REQUEST\" or \"routeArn\" in event:\n        return authorize(event)\n    if (event.get(\"requestContext\") or {}).get(\"http\"):\n        return get_me(event)\n    return http.bad_request(\"unrecognized event shape for the authorizer function\")\n"
    },
    {
      "file": "template.yaml",
      "title": "Cognito pool, MFA, RLS personas",
      "note": "MFA and the role-to-row-scope mapping are declared configuration, not a promise.",
      "startLine": 585,
      "endLine": 660,
      "totalLines": 4153,
      "code": "      PubliclyAccessible: false\n\n  # ===========================================================================\n  # (4) Cognito - OPTIONAL TOTP. Team accounts remain password-only; the\n  #     dedicated formal presenter account enables TOTP per user.\n  #     that map 1:1 to the RLS personas in docs/CONTRACTS.md:\n  #       compass-poweruser -> org_unit ONR-Corporate (sees all rows)\n  #       compass-viewer    -> org_unit Code-30       (sees Code-30 only)\n  # ===========================================================================\n\n  UserPool:\n    Type: AWS::Cognito::UserPool\n    Properties:\n      UserPoolName: !Sub \"${AWS::StackName}-users\"\n      AutoVerifiedAttributes: [email]\n      UsernameAttributes: [email]\n      MfaConfiguration: OPTIONAL\n      EnabledMfas: [SOFTWARE_TOKEN_MFA]\n      AdminCreateUserConfig:\n        AllowAdminCreateUserOnly: true   # demo personas are seeded, no self-signup\n      Policies:\n        PasswordPolicy:\n          MinimumLength: 16\n          RequireLowercase: true\n          RequireUppercase: true\n          RequireNumbers: true\n          RequireSymbols: true\n          TemporaryPasswordValidityDays: 7\n      Schema:\n        - Name: email\n          AttributeDataType: String\n          Required: true\n          Mutable: true\n\n  UserPoolDomain:\n    Type: AWS::Cognito::UserPoolDomain\n    Properties:\n      Domain: !If\n        - HasCognitoDomainPrefix\n        - !Ref CognitoDomainPrefix\n        - !Sub \"${AWS::StackName}-${AWS::AccountId}\"\n      UserPoolId: !Ref UserPool\n\n  PowerUserGroup:\n    Type: AWS::Cognito::UserPoolGroup\n    Properties:\n      GroupName: compass-poweruser\n      Description: \"Portfolio power user - org_unit ONR-Corporate, sees the whole portfolio\"\n      UserPoolId: !Ref UserPool\n      Precedence: 10\n\n  ViewerGroup:\n    Type: AWS::Cognito::UserPoolGroup\n    Properties:\n      GroupName: compass-viewer\n      Description: \"Program-office viewer - org_unit Code-30, RLS-scoped to Code-30 rows\"\n      UserPoolId: !Ref UserPool\n      Precedence: 20\n\n  WebClient:\n    Type: AWS::Cognito::UserPoolClient\n    Properties:\n      UserPoolId: !Ref UserPool\n      ClientName: !Sub \"${AWS::StackName}-web\"\n      GenerateSecret: false\n      SupportedIdentityProviders: [COGNITO]\n      AllowedOAuthFlows: [code]\n      AllowedOAuthFlowsUserPoolClient: true\n      AllowedOAuthScopes: [openid, email, profile]\n      # localhost is always registered; the CloudFront URLs are parameters\n      # filled on the SECOND deploy (the domain does not exist until the first).\n      # /login/ (with the trailing slash) is the SPA's actual redirect_uri:\n      # see frontend/lib/auth/providers.tsx and next.config.ts trailingSlash.\n      CallbackURLs: !If\n        - HasWebCallbackUrl\n        - - http://localhost:3000/login/"
    }
  ],
  "/licenses/": [
    {
      "file": "src/functions/license/app.py",
      "title": "License register and renewal alerts",
      "note": "Renewal urgency, seat utilization, and effective status - the prompt (e) methodology as code.",
      "startLine": 76,
      "endLine": 210,
      "totalLines": 309,
      "code": "def renewal_alert(days: Optional[int], renews_on: Any) -> Dict[str, Any]:\n    \"\"\"Classify a renewal date into ``{level, message, days_to_renewal}``.\"\"\"\n    when = _iso(renews_on)\n    if days is None:\n        return {\n            \"level\": \"warning\",\n            \"days_to_renewal\": None,\n            \"message\": \"No renewal date recorded - term cannot be verified.\",\n        }\n    if days < 0:\n        return {\n            \"level\": \"expired\",\n            \"days_to_renewal\": days,\n            \"message\": f\"Term ended {abs(days)} day(s) ago on {when}. \"\n                       \"Any pipeline still calling this vendor is out of entitlement.\",\n        }\n    if days <= CRITICAL_DAYS:\n        return {\n            \"level\": \"critical\",\n            \"days_to_renewal\": days,\n            \"message\": f\"Renews in {days} day(s) on {when} - inside the \"\n                       f\"{CRITICAL_DAYS}-day critical window; start the renewal action now.\",\n        }\n    if days <= WARNING_DAYS:\n        return {\n            \"level\": \"warning\",\n            \"days_to_renewal\": days,\n            \"message\": f\"Renews in {days} day(s) on {when} - inside the \"\n                       f\"{WARNING_DAYS}-day planning window.\",\n        }\n    return {\n        \"level\": \"ok\",\n        \"days_to_renewal\": days,\n        \"message\": f\"Renews {when} ({days} day(s) out).\",\n    }\n\n\ndef seat_alert(used: Optional[int], total: Optional[int]) -> Dict[str, Any]:\n    \"\"\"Classify seat utilisation. Unmetered licenses (total 0/None) are ``ok``.\"\"\"\n    if not total:\n        return {\n            \"level\": \"ok\",\n            \"utilization_pct\": None,\n            \"seats_available\": None,\n            \"message\": \"Unmetered entitlement - no seat cap.\",\n        }\n    used = used or 0\n    pct = round(100.0 * used / total, 1)\n    available = total - used\n    if used >= total:\n        level = \"critical\"\n        message = (f\"All {total} seat(s) assigned - new users cannot be onboarded \"\n                   \"without buying seats.\")\n    elif pct >= SEAT_WARN_RATIO * 100:\n        level = \"warning\"\n        message = f\"{used} of {total} seats assigned ({pct}%) - {available} left.\"\n    else:\n        level = \"ok\"\n        message = f\"{used} of {total} seats assigned ({pct}%).\"\n    return {\n        \"level\": level,\n        \"utilization_pct\": pct,\n        \"seats_available\": available,\n        \"message\": message,\n    }\n\n\ndef effective_status(stored: Optional[str], renewal_level: str) -> str:\n    \"\"\"Lifecycle state the dates support, with ``suspended`` always winning.\n\n    Returns one of active|expiring|expired|suspended (the frontend's union).\n    \"\"\"\n    if (stored or \"\").lower() == \"suspended\":\n        return \"suspended\"\n    if renewal_level == \"expired\":\n        return \"expired\"\n    if renewal_level in (\"critical\", \"warning\"):\n        return \"expiring\"\n    return \"active\"\n\n\ndef load_licenses(conn) -> List[Dict[str, Any]]:\n    \"\"\"Read the register and score every row. ``CURRENT_DATE`` is the DB clock.\"\"\"\n    with conn.cursor() as cur:\n        cur.execute(\n            \"\"\"\n            SELECT id, vendor, product, datasets, entitlements,\n                   seats_used, seats_total, renews_on, owner, status,\n                   (renews_on - CURRENT_DATE) AS days_to_renewal\n              FROM licenses\n             ORDER BY renews_on NULLS LAST, vendor, product\n            \"\"\"\n        )\n        cols = [c.name for c in cur.description]\n        rows = [dict(zip(cols, r)) for r in cur.fetchall()]\n\n    out: List[Dict[str, Any]] = []\n    for r in rows:\n        days = _int(r[\"days_to_renewal\"])\n        ren = renewal_alert(days, r[\"renews_on\"])\n        seats = seat_alert(_int(r[\"seats_used\"]), _int(r[\"seats_total\"]))\n        out.append(\n            {\n                \"id\": _int(r[\"id\"]),\n                \"vendor\": r[\"vendor\"],\n                \"product\": r[\"product\"],\n                \"datasets\": list(r[\"datasets\"] or []),\n                \"entitlements\": r[\"entitlements\"],\n                \"seats_used\": _int(r[\"seats_used\"]) or 0,\n                \"seats_total\": _int(r[\"seats_total\"]) or 0,\n                \"renews_on\": _iso(r[\"renews_on\"]),\n                \"owner\": r[\"owner\"],\n                # `status` is the lifecycle the dates support (what the UI shows);\n                # `status_stored` is what the register says, so drift is visible.\n                \"status\": effective_status(r[\"status\"], ren[\"level\"]),\n                \"status_stored\": r[\"status\"],\n                \"days_to_renewal\": days,\n                \"renewal_alert\": ren,\n                \"seat_alert\": seats,\n                \"needs_action\": ren[\"level\"] in (\"expired\", \"critical\")\n                or seats[\"level\"] == \"critical\",\n            }\n        )\n    return out\n\n\ndef summarize(licenses: List[Dict[str, Any]]) -> Dict[str, Any]:\n    by_status: Dict[str, int] = {}\n    for lic in licenses:\n        by_status[lic[\"status\"]] = by_status.get(lic[\"status\"], 0) + 1\n    seats_total = sum(lic[\"seats_total\"] for lic in licenses)\n    seats_used = sum(lic[\"seats_used\"] for lic in licenses)\n    return {\n        \"total\": len(licenses),\n        \"by_status\": by_status,"
    }
  ],
  "/admin/delivery/": [
    {
      "file": ".github/workflows/pipeline.yml",
      "title": "Golden pipeline consumption (org standard)",
      "note": "The project consumes the organization's golden pipeline as a SHA-pinned reusable workflow - this file only routes triggers and overrides project variables.",
      "startLine": 1,
      "endLine": 72,
      "totalLines": 72,
      "code": "name: Enterprise Golden CI/CD Pipeline\n\non:\n  push:\n    branches:\n      - 'iac'\n      - 'dev'\n      - 'development'\n      - 'feature/**'\n      - 'fix/**'\n      - 'codex/public-onr-intelligence'\n      - 'main'\n    tags:\n      - 'v[0-9]+.[0-9]+.[0-9]+*'\n  pull_request:\n    branches:\n      - 'iac'\n      - 'dev'\n      - 'development'\n      - 'codex/public-onr-intelligence'\n      - 'main'\n  workflow_dispatch:\n    inputs:\n      action:\n        description: 'Action to perform: plan_only (default), apply, or destroy'\n        required: false\n        default: 'plan_only'\n        type: choice\n        options:\n          - 'plan_only'\n          - 'apply'\n          - 'destroy'\n      release_tag:\n        description: 'Release Tag to deploy (e.g., v1.0.0)'\n        required: false\n      deploy_type:\n        description: 'Deploy Type: auto, all, container, opentofu, databricks_dab, or comma-separated'\n        required: false\n        default: 'auto'\n      target_env:\n        description: 'Override Target Environment (dev, test, prod)'\n        required: false\n        default: 'auto'\n\npermissions:\n  id-token: write\n  contents: read\n  security-events: write\n  packages: write\n  actions: read\n\nenv:\n  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: \"true\"\n\njobs:\n  golden-pipeline:\n    # Pinned to golden-pipelines dev @ 2026-08-19 (51b0327...). A mutable @dev\n    # ref would run whatever lands on that branch with this repo's inherited\n    # secrets; bump the SHA deliberately when adopting pipeline updates.\n    uses: SatsyilCorp/golden-pipelines/.github/workflows/enterprise-golden-pipeline.yml@51b03273b59f0003d4b7383905d76fcaffa3a822\n    with:\n      action: ${{ inputs.action || 'plan_only' }}\n      # Manual dispatch without an explicit environment targets dev, never prod.\n      target_env: ${{ inputs.target_env != '' && inputs.target_env != 'auto' && inputs.target_env || (startsWith(github.ref, 'refs/tags/v') && 'release') || (github.ref == 'refs/heads/main' && 'test') || 'dev' }}\n      deploy_type: ${{ inputs.deploy_type || 'auto' }}\n      release_tag: ${{ inputs.release_tag || (startsWith(github.ref, 'refs/tags/v') && github.ref_name || '') }}\n      infra_dir: 'infra/terraform'\n      src_dir: 'src'\n      databricks_dir: 'databricks'\n      dockerfile_path: 'Dockerfile'\n    secrets: inherit\n"
    },
    {
      "file": ".github/workflows/devsecops.yml",
      "title": "CloudFormation policy gates",
      "note": "The gates the golden standard cannot express yet: SAM validation, policy-as-code, the STIG evidence index, and the enforcing secret scan.",
      "startLine": 1,
      "endLine": 64,
      "totalLines": 64,
      "code": "name: CloudFormation policy gates\n\n# Compass-specific gates the golden pipeline cannot express yet: the org\n# standard (SatsyilCorp/golden-pipelines, consumed by pipeline.yml) owns SAST,\n# dependency, secret, SBOM, and Terraform/OpenTofu scanning. This file keeps\n# only the CloudFormation side - template validation, policy-as-code, and the\n# STIG-aligned evidence index for template.yaml. When golden-pipelines gains a\n# CloudFormation mode, this file retires.\n\non:\n  pull_request:\n  push:\n    branches: [main]\n  workflow_dispatch:\n\npermissions:\n  contents: read\n\nconcurrency:\n  group: devsecops-${{ github.ref }}\n  cancel-in-progress: true\n\njobs:\n  cloudformation-policy:\n    name: SAM validation, policy-as-code, and STIG evidence\n    runs-on: ubuntu-latest\n    steps:\n      - name: Check out source\n        uses: actions/checkout@v4\n\n      # Kept beside the golden pipeline deliberately: golden's secret gate is\n      # currently non-blocking (`|| true`) and unredacted, so this remains the\n      # one enforcing, redacted scan under the reviewed policy. Retires when\n      # golden-pipelines' security-secrets action blocks and redacts.\n      - name: Secret scan\n        run: docker run --rm -v \"$PWD:/repo\" ghcr.io/gitleaks/gitleaks:v8.24.2 detect --source=/repo --config=/repo/.gitleaks.toml --no-banner --redact\n\n      - name: Set up Python\n        uses: actions/setup-python@v5\n        with:\n          python-version: \"3.12\"\n\n      - name: Set up AWS SAM\n        uses: aws-actions/setup-sam@v2\n\n      - name: SAM and CloudFormation validation\n        run: sam validate --lint\n\n      - name: Install CloudFormation Guard\n        run: cargo install cfn-guard --version 3.1.2 --locked\n\n      - name: Strict policy-as-code validation\n        run: cfn-guard validate --data template.yaml --rules security/policies/compass.guard --show-summary all\n\n      - name: Validate STIG-aligned evidence index\n        run: python scripts/validate_stig_controls.py --output artifacts/security/stig-evidence.json\n\n      - name: Publish policy evidence\n        uses: actions/upload-artifact@v4\n        with:\n          name: policy-and-stig-evidence\n          path: artifacts/security/stig-evidence.json\n          retention-days: 30\n"
    },
    {
      "file": "template.yaml",
      "title": "The environment as CloudFormation",
      "note": "The single template the whole 100+-resource environment is provisioned from - parameters are the operational policy knobs.",
      "startLine": 1,
      "endLine": 80,
      "totalLines": 4153,
      "code": "AWSTemplateFormatVersion: \"2010-09-09\"\nTransform: AWS::Serverless-2016-10-31\nDescription: >\n  Compass - US Navy/ONR S&T Portfolio Intelligence demo (single stack,\n  us-east-1). Everything is created in-template for reproducibility: a minimal\n  VPC + Aurora Serverless v2 PostgreSQL (RLS-enforced), Cognito (per-user MFA\n  optional so team accounts stay password-only and a presenter proves TOTP)\n  with a JWT-guarded HttpApi, one Lambda per src/functions dir on a\n  shared CommonLayer, an Express intake state machine fired by S3 -> EventBridge,\n  an on-demand Kinesis ticker stream, a private S3 + CloudFront web tier behind\n  a CLOUDFRONT-scope WAF, and a customer-managed KMS key over data at rest.\n  The core portfolio uses synthetic data. The isolated public-intelligence\n  adapter can serve checksummed, PII-minimized public evidence. No CUI is used.\n  NOTE: the WAF WebACL is Scope CLOUDFRONT, so this stack MUST deploy in\n  us-east-1 (pinned in samconfig.toml).\n\n# -----------------------------------------------------------------------------\n# Parameters\n# -----------------------------------------------------------------------------\nParameters:\n\n  WebCallbackUrl:\n    Type: String\n    Default: \"\"\n    Description: >\n      Deployed OAuth callback URL. The SPA (react-oidc-context, trailingSlash\n      static export) uses /login/ as its redirect_uri, so use e.g.\n      https://dXXXX.cloudfront.net/login/. Empty on the FIRST deploy\n      (CloudFront domain not yet known); fill it in samconfig.toml and\n      redeploy. http://localhost:3000/login/ is always registered for local dev.\n\n  WebLogoutUrl:\n    Type: String\n    Default: \"\"\n    Description: >\n      Deployed post-logout return URL, e.g. https://dXXXX.cloudfront.net/login/.\n      Same two-deploy dance as WebCallbackUrl.\n\n  WebOrigin:\n    Type: String\n    Default: \"\"\n    Description: >\n      Deployed web origin for API CORS, e.g. https://dXXXX.cloudfront.net.\n      Empty on the first deploy; http://localhost:3000 is always allowed.\n\n  WebCustomDomainName:\n    Type: String\n    Default: \"\"\n    Description: Optional Route 53 name presented by CloudFront.\n\n  WebCertificateArn:\n    Type: String\n    Default: \"\"\n    Description: Optional us-east-1 ACM certificate ARN for the custom domain.\n\n  WebHostedZoneId:\n    Type: String\n    Default: \"\"\n    Description: Optional public Route 53 hosted zone ID for the custom domain.\n\n  CognitoDomainPrefix:\n    Type: String\n    Default: \"\"\n    Description: >\n      Globally-unique Cognito hosted-UI domain prefix. Leave empty to use\n      compass-demo-<account-id>.\n\n  ExportMaxRows:\n    Type: Number\n    Default: 5000\n    Description: >\n      Aggregation-guard threshold for POST /export - requests above this row\n      count return HTTP 428 (approval required) unless an approval token is\n      attached (see docs/CONTRACTS.md).\n\n  WafRateLimit:\n    Type: Number\n    Default: 2000\n    Description: 5-minute request cap per source IP on the WAF rate rule.\n"
    },
    {
      "file": "infra/terraform/main.tf",
      "title": "The ML slice as Terraform",
      "note": "The same architecture expressed in a second IaC dialect - the anti-lock-in proof.",
      "startLine": 1,
      "endLine": 70,
      "totalLines": 396,
      "code": "locals {\n  name = \"${var.project_name}-${var.environment}-document-ml\"\n  routes = toset([\n    \"GET /documents/runs\",\n    \"GET /documents/runs/{run_id}\",\n    \"GET /ml/models\",\n    \"GET /ml/ops/evidence\",\n    \"POST /documents/uploads\",\n    \"POST /ml/drift/evaluate\",\n    \"POST /ml/models/{version}/deploy\",\n    \"POST /ml/train\",\n  ])\n}\n\nresource \"aws_dynamodb_table\" \"document_ml\" {\n  name         = local.name\n  billing_mode = \"PAY_PER_REQUEST\"\n  hash_key     = \"pk\"\n  range_key    = \"sk\"\n\n  attribute {\n    name = \"pk\"\n    type = \"S\"\n  }\n\n  attribute {\n    name = \"sk\"\n    type = \"S\"\n  }\n\n  attribute {\n    name = \"gsi1pk\"\n    type = \"S\"\n  }\n\n  attribute {\n    name = \"gsi1sk\"\n    type = \"S\"\n  }\n\n  global_secondary_index {\n    name            = \"by-type\"\n    hash_key        = \"gsi1pk\"\n    range_key       = \"gsi1sk\"\n    projection_type = \"ALL\"\n  }\n\n  point_in_time_recovery {\n    enabled = true\n  }\n\n  server_side_encryption {\n    enabled     = true\n    kms_key_arn = var.kms_key_arn\n  }\n}\n\nresource \"aws_sagemaker_model_package_group\" \"document_classifier\" {\n  model_package_group_name        = \"${local.name}-classifier\"\n  model_package_group_description = \"Governed Compass document classifier versions\"\n}\n\ndata \"aws_iam_policy_document\" \"sagemaker_assume\" {\n  statement {\n    effect  = \"Allow\"\n    actions = [\"sts:AssumeRole\"]\n\n    principals {\n      type        = \"Service\"\n      identifiers = [\"sagemaker.amazonaws.com\"]"
    }
  ],
  "/ingest/": [
    {
      "file": "src/functions/intake/pipeline.py",
      "title": "Ingestion logic (Python)",
      "note": "Open-source Python doing the work inside each state - hashing, normalization, quality.",
      "startLine": 1,
      "endLine": 160,
      "totalLines": 582,
      "code": "\"\"\"Ingest pipeline stages: S3 → ``grants_raw`` (Fetch) → ``grants_curated`` (Persist).\n\nThese are the two intake-owned states of ``statemachines/intake.asl.yaml``. The\nquality gate sits between them and is a separate Lambda.\n\nData flow\n---------\n``Fetch`` reads the dropped object, parses and normalizes it\n(``normalize.py``), and upserts one ``grants_raw`` row per source record. It\nreturns only a small manifest - batch id, run id, counts - never the records\nthemselves, so the Step Functions payload stays far below the 256 KB state\nlimit no matter how large the drop is. Every later stage re-reads the rows it\nneeds from ``grants_raw`` by ``batch_id``.\n\n``Persist`` curates the rows the quality gate marked ``passed``, optionally\nembedding each abstract with Titan v2 for the RAG route, and inserts them into\n``grants_curated``.\n\nIdentity and RLS\n----------------\nThis is machine-to-machine work with no end user, so the pipeline runs under\nthe corporate org context (``compass.org_unit = 'ONR-Corporate'``). That is the\none context the ``grants_curated`` INSERT policy accepts for rows belonging to\n*any* unit - a batch legitimately contains Code-30, Code-32 and Code-34 awards.\nIt is still the real policy doing the work: the connection is\n``compass_app`` (``compass_common.db`` sets the role), the table is\n``FORCE ROW LEVEL SECURITY``, and the GUC is set with ``SET LOCAL`` inside the\ntransaction so it cannot leak to the next invocation. The corporate context is\nalso what makes ``INSERT … RETURNING id`` legal here - under RLS, ``RETURNING``\nreads the new row back through the SELECT policy, which a unit context would\nrefuse for another unit's award.\n\nIdempotency\n-----------\nThe batch id is deterministic (from the drop envelope, else derived from the\nobject key), and every write is an upsert keyed on ``(batch_id, row_index)``\nfor raw rows, ``grant_no`` for curated rows, and the primary keys for lineage.\nRe-dropping the same file therefore re-runs the same batch instead of doubling\nit - which matters, because a demo drops the same file more than once.\n\"\"\"\nfrom __future__ import annotations\n\nimport json\nimport hashlib\nimport os\nfrom typing import Any, Dict, List, Optional, Sequence, Tuple\n\nfrom compass_common import audit, config, db, operational_evidence\n\nimport normalize\n\nPIPELINE_ORG_UNIT = config.CORPORATE_ORG_UNIT     # \"ONR-Corporate\"\nPIPELINE_ACTOR = \"compass-intake-pipeline\"\n\n# EventBridge is restricted to ``drops/``. The skip list remains as defense in\n# depth for direct function or state-machine invocations with another key.\nDEFAULT_SKIP_PREFIXES = \"demo-stage/,exports/,rmf/,quarantine/,tmp/,athena-results/\"\nINGESTIBLE_SUFFIXES = (\".json\", \".jsonl\", \".ndjson\")\nDEFAULT_MAX_OBJECT_BYTES = 8 * 1024 * 1024\n\n_S3 = None\n\n\ndef run_id_for(batch_id: str) -> str:\n    \"\"\"``run-<batch_id>`` - the convention the catalog and lineage routes join on.\"\"\"\n    return f\"run-{batch_id}\"\n\n\ndef _s3():\n    global _S3\n    if _S3 is None:\n        import boto3\n\n        _S3 = boto3.client(\"s3\", region_name=config.aws_region())\n    return _S3\n\n\ndef _skip_prefixes() -> Tuple[str, ...]:\n    raw = os.environ.get(\"INGEST_SKIP_PREFIXES\", DEFAULT_SKIP_PREFIXES)\n    return tuple(p.strip() for p in raw.split(\",\") if p.strip())\n\n\ndef is_ingestible(key: str, size: Optional[int] = None) -> Tuple[bool, str]:\n    \"\"\"Should this object start an ingest run? Returns (ok, reason).\"\"\"\n    if not key:\n        return False, \"empty object key\"\n    if key.endswith(\"/\"):\n        return False, \"object key is a folder marker\"\n    for prefix in _skip_prefixes():\n        if key.startswith(prefix):\n            return False, f\"key is under the non-ingest prefix {prefix!r}\"\n    if not key.lower().endswith(INGESTIBLE_SUFFIXES):\n        return False, f\"unsupported extension (expected one of {', '.join(INGESTIBLE_SUFFIXES)})\"\n    max_bytes = int(os.environ.get(\"INGEST_MAX_OBJECT_BYTES\", DEFAULT_MAX_OBJECT_BYTES))\n    if size is not None and size > max_bytes:\n        return False, f\"object is {size} bytes, above the {max_bytes}-byte ingest limit\"\n    return True, \"ingestible\"\n\n\ndef read_object(bucket: str, key: str) -> str:\n    \"\"\"Read an S3 object as UTF-8 text.\n\n    EventBridge S3 events deliver the key unencoded, but the older notification\n    shape URL-encodes it; if the first GET 404s on a key containing escapes we\n    retry with the decoded form rather than failing a real drop.\n    \"\"\"\n    from urllib.parse import unquote_plus\n\n    client = _s3()\n    try:\n        obj = client.get_object(Bucket=bucket, Key=key)\n    except client.exceptions.NoSuchKey:\n        decoded = unquote_plus(key)\n        if decoded == key:\n            raise\n        obj = client.get_object(Bucket=bucket, Key=decoded)\n    return obj[\"Body\"].read().decode(\"utf-8\")\n\n\n# --------------------------------------------------------------------------- #\n# Lineage\n# --------------------------------------------------------------------------- #\ndef upsert_lineage_nodes(cur, run_id: str, nodes: Sequence[Dict[str, Any]]) -> None:\n    \"\"\"Write ``lineage_nodes`` for this run (upsert - a re-run refreshes meta).\"\"\"\n    for node in nodes:\n        cur.execute(\n            \"INSERT INTO lineage_nodes (run_id, node_id, kind, label, meta_jsonb) \"\n            \"VALUES (%s, %s, %s, %s, %s::jsonb) \"\n            \"ON CONFLICT (run_id, node_id) DO UPDATE \"\n            \"SET kind = EXCLUDED.kind, label = EXCLUDED.label, \"\n            \"    meta_jsonb = EXCLUDED.meta_jsonb\",\n            (\n                run_id,\n                node[\"node_id\"],\n                node[\"kind\"],\n                node[\"label\"],\n                json.dumps(node.get(\"meta\") or {}, default=str),\n            ),\n        )\n\n\ndef upsert_lineage_edges(cur, run_id: str, edges: Sequence[Tuple[str, str]]) -> None:\n    for from_node, to_node in edges:\n        cur.execute(\n            \"INSERT INTO lineage_edges (run_id, from_node, to_node) VALUES (%s, %s, %s) \"\n            \"ON CONFLICT (run_id, from_node, to_node) DO NOTHING\",\n            (run_id, from_node, to_node),\n        )\n\n\n# --------------------------------------------------------------------------- #\n# Fetch stage\n# --------------------------------------------------------------------------- #\ndef _upsert_raw_rows(\n    cur, batch_id: str, source_file: str, envelopes: Sequence[Dict[str, Any]]\n) -> Tuple[int, int]:\n    \"\"\"Upsert one ``grants_raw`` row per record, keyed (batch_id, row_index).\n\n    ``grants_raw`` has no natural key and ``compass_app`` holds no DELETE grant,\n    so re-ingest is an explicit update-then-insert per row. Batches here are"
    }
  ],
  "/catalog/": [
    {
      "file": "db/migrations/002_rls.sql",
      "title": "Row-level security policy",
      "note": "FORCE ROW LEVEL SECURITY - the role-to-rows rule as source-controlled SQL.",
      "startLine": 5,
      "endLine": 58,
      "totalLines": 58,
      "code": "SET search_path TO compass, public;\n\n-- Least-privilege runtime role used by all application Lambdas.\n-- Owner (migrator) creates objects; compass_app only reads/writes rows.\nDO $$\nBEGIN\n  IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'compass_app') THEN\n    CREATE ROLE compass_app NOLOGIN;   -- membership granted to the IAM/db login\n  END IF;\nEND $$;\n\nGRANT USAGE ON SCHEMA compass TO compass_app;\nGRANT SELECT, INSERT, UPDATE ON ALL TABLES IN SCHEMA compass TO compass_app;\nGRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA compass TO compass_app;\nALTER DEFAULT PRIVILEGES IN SCHEMA compass\n  GRANT SELECT, INSERT, UPDATE ON TABLES TO compass_app;\nALTER DEFAULT PRIVILEGES IN SCHEMA compass\n  GRANT USAGE, SELECT ON SEQUENCES TO compass_app;\n\n-- Enable + FORCE RLS on the curated portfolio.\nALTER TABLE grants_curated ENABLE ROW LEVEL SECURITY;\nALTER TABLE grants_curated FORCE ROW LEVEL SECURITY;   -- <-- the load-bearing line\n\n-- Context: each request runs `SET LOCAL compass.org_unit = '<claim>'`.\n-- Corporate org sees all rows; a unit org sees only its own.\nDROP POLICY IF EXISTS grants_rls_read ON grants_curated;\nCREATE POLICY grants_rls_read ON grants_curated\n  FOR SELECT\n  USING (\n    current_setting('compass.org_unit', true) = 'ONR-Corporate'\n    OR org_unit = current_setting('compass.org_unit', true)\n  );\n\nDROP POLICY IF EXISTS grants_rls_write ON grants_curated;\nCREATE POLICY grants_rls_write ON grants_curated\n  FOR INSERT\n  WITH CHECK (\n    current_setting('compass.org_unit', true) = 'ONR-Corporate'\n    OR org_unit = current_setting('compass.org_unit', true)\n  );\n\n-- Column-Level Security (CLS): viewers cannot read the dollar amount.\n-- Revoke the column from compass_app, expose a masked view for viewers.\nREVOKE SELECT (amount_usd) ON grants_curated FROM compass_app;\nGRANT SELECT (id, grant_no, title, abstract, program_area, fiscal_year,\n              awardee, org_unit, classification_band, batch_id, created_at)\n  ON grants_curated TO compass_app;\n\nCREATE OR REPLACE VIEW grants_curated_corp AS\n  SELECT * FROM grants_curated;   -- amount visible only where column grant allows\n\nINSERT INTO _schema_migrations(name) VALUES ('002_rls')\n  ON CONFLICT (name) DO NOTHING;\n"
    }
  ],
  "/admin/lineage/": [
    {
      "file": "src/functions/operations/app.py",
      "title": "Lineage readback (server-side)",
      "note": "The per-run lineage the visual trace renders - reconstructed from receipts, stage by stage.",
      "startLine": 180,
      "endLine": 280,
      "totalLines": 497,
      "code": "\n\ndef _is_live_public_evidence_class(value: Any) -> bool:\n    evidence_class = _normalized_evidence_class(value)\n    return bool(\n        evidence_class == \"operational-control\"\n        or evidence_class == \"public\"\n        or (evidence_class and evidence_class.startswith(\"public-\"))\n    )\n\n\ndef _classify_lineage_stages(stages: List[Dict[str, Any]]) -> List[Dict[str, Any]]:\n    by_run: Dict[str, List[Dict[str, Any]]] = {}\n    for stage in stages:\n        run_id = str(stage.get(\"run_id\") or \"\")\n        if run_id:\n            by_run.setdefault(run_id, []).append(stage)\n\n    run_classes = {\n        run_id: _run_evidence_class(run_stages)\n        for run_id, run_stages in by_run.items()\n    }\n    classified = []\n    for stage in stages:\n        run_id = str(stage.get(\"run_id\") or \"\")\n        evidence_class = _inferred_evidence_class(stage, fallback=\"unclassified\")\n        if evidence_class == \"unclassified\":\n            evidence_class = run_classes.get(run_id, \"unclassified\")\n        classified.append({**stage, \"evidence_class\": evidence_class})\n    return classified\n\n\ndef _classify_signal(signal: Dict[str, Any]) -> Dict[str, Any]:\n    return {\n        **signal,\n        \"evidence_class\": _inferred_evidence_class(\n            signal,\n            fallback=\"operational-control\",\n        ),\n    }\n\n\ndef list_signals(limit: int = 50) -> Dict[str, Any]:\n    signals = [\n        _classify_signal(signal)\n        for signal in _query_index(\"SIGNAL\", limit=limit)\n    ]\n    unresolved_run_ids = {\n        str(signal.get(\"run_id\") or \"\")\n        for signal in signals\n        if signal.get(\"evidence_class\") == \"operational-control\"\n        and signal.get(\"run_id\")\n    }\n    if unresolved_run_ids:\n        lineage_stages = _classify_lineage_stages(\n            _query_index(\"LINEAGE\", limit=MAX_STAGES)\n        )\n        lineage_classes = {\n            run_id: _run_evidence_class(\n                [\n                    stage\n                    for stage in lineage_stages\n                    if str(stage.get(\"run_id\") or \"\") == run_id\n                ]\n            )\n            for run_id in unresolved_run_ids\n        }\n        signals = [\n            {\n                **signal,\n                \"evidence_class\": lineage_classes.get(\n                    str(signal.get(\"run_id\") or \"\"),\n                    \"operational-control\",\n                ),\n            }\n            if signal.get(\"evidence_class\") == \"operational-control\"\n            and lineage_classes.get(str(signal.get(\"run_id\") or \"\"))\n            not in {None, \"unclassified\"}\n            else signal\n            for signal in signals\n        ]\n    signals = [\n        signal\n        for signal in signals\n        if _is_live_public_evidence_class(signal.get(\"evidence_class\"))\n    ]\n    return {\n        \"contract\": \"compass.operational-signals.v1\",\n        \"mode\": \"live\",\n        \"evidence_scope\": \"public-only\",\n        \"generated_at\": datetime.now(timezone.utc).isoformat(),\n        \"signals\": signals,\n        \"unacknowledged\": sum(1 for item in signals if _needs_attention(item)),\n        \"delivery_disclosure\": (\n            \"In-app evidence is authoritative. SNS means the event was published to \"\n            \"the protected topic; email requires a separately confirmed subscription.\"\n        ),\n    }\n\n\ndef list_lineage(limit: int = 100) -> Dict[str, Any]:"
    }
  ],
  "/catalog/lineage/": [
    {
      "file": "db/migrations/002_rls.sql",
      "title": "Row-level security policy",
      "note": "FORCE ROW LEVEL SECURITY - the role-to-rows rule as source-controlled SQL.",
      "startLine": 5,
      "endLine": 58,
      "totalLines": 58,
      "code": "SET search_path TO compass, public;\n\n-- Least-privilege runtime role used by all application Lambdas.\n-- Owner (migrator) creates objects; compass_app only reads/writes rows.\nDO $$\nBEGIN\n  IF NOT EXISTS (SELECT 1 FROM pg_roles WHERE rolname = 'compass_app') THEN\n    CREATE ROLE compass_app NOLOGIN;   -- membership granted to the IAM/db login\n  END IF;\nEND $$;\n\nGRANT USAGE ON SCHEMA compass TO compass_app;\nGRANT SELECT, INSERT, UPDATE ON ALL TABLES IN SCHEMA compass TO compass_app;\nGRANT USAGE, SELECT ON ALL SEQUENCES IN SCHEMA compass TO compass_app;\nALTER DEFAULT PRIVILEGES IN SCHEMA compass\n  GRANT SELECT, INSERT, UPDATE ON TABLES TO compass_app;\nALTER DEFAULT PRIVILEGES IN SCHEMA compass\n  GRANT USAGE, SELECT ON SEQUENCES TO compass_app;\n\n-- Enable + FORCE RLS on the curated portfolio.\nALTER TABLE grants_curated ENABLE ROW LEVEL SECURITY;\nALTER TABLE grants_curated FORCE ROW LEVEL SECURITY;   -- <-- the load-bearing line\n\n-- Context: each request runs `SET LOCAL compass.org_unit = '<claim>'`.\n-- Corporate org sees all rows; a unit org sees only its own.\nDROP POLICY IF EXISTS grants_rls_read ON grants_curated;\nCREATE POLICY grants_rls_read ON grants_curated\n  FOR SELECT\n  USING (\n    current_setting('compass.org_unit', true) = 'ONR-Corporate'\n    OR org_unit = current_setting('compass.org_unit', true)\n  );\n\nDROP POLICY IF EXISTS grants_rls_write ON grants_curated;\nCREATE POLICY grants_rls_write ON grants_curated\n  FOR INSERT\n  WITH CHECK (\n    current_setting('compass.org_unit', true) = 'ONR-Corporate'\n    OR org_unit = current_setting('compass.org_unit', true)\n  );\n\n-- Column-Level Security (CLS): viewers cannot read the dollar amount.\n-- Revoke the column from compass_app, expose a masked view for viewers.\nREVOKE SELECT (amount_usd) ON grants_curated FROM compass_app;\nGRANT SELECT (id, grant_no, title, abstract, program_area, fiscal_year,\n              awardee, org_unit, classification_band, batch_id, created_at)\n  ON grants_curated TO compass_app;\n\nCREATE OR REPLACE VIEW grants_curated_corp AS\n  SELECT * FROM grants_curated;   -- amount visible only where column grant allows\n\nINSERT INTO _schema_migrations(name) VALUES ('002_rls')\n  ON CONFLICT (name) DO NOTHING;\n"
    }
  ],
  "/admin/mlops/": [
    {
      "file": "src/functions/analytics/topic_model.py",
      "title": "Topic model (open-source Python)",
      "note": "The analytical routine Element 5 triggers - versioned, cited in every run receipt.",
      "startLine": 1,
      "endLine": 120,
      "totalLines": 439,
      "code": "\"\"\"Lightweight topic modeling over grant abstracts - numpy-only TF-IDF + NMF.\n\nAlgorithm (exact, so evaluators can check the math)\n---------------------------------------------------\n1. **Tokenize**: lowercase, extract ``[a-z][a-z0-9-]{2,}`` tokens, drop a\n   standard English stopword list plus S&T boilerplate (\"effort\", \"novel\",\n   \"demonstrate\", ...) that the templated abstracts share across every topic.\n2. **Vocabulary**: keep terms appearing in >= ``min_df`` documents and <=\n   ``max_df_ratio`` of documents (boilerplate guard), capped at ``max_vocab``\n   terms by total corpus frequency.\n3. **TF-IDF**: ``tfidf = tf * (ln((1+N)/(1+df)) + 1)`` (smoothed, sklearn-style\n   idf), then L2-normalize each document row.\n4. **NMF**: factor the docs×terms matrix ``X ≈ W·H`` (``W``: docs×k,\n   ``H``: k×terms, both non-negative) with Lee & Seung multiplicative updates\n   minimizing the Frobenius norm::\n\n       H <- H * (WᵀX) / (WᵀWH + eps)\n       W <- W * (XHᵀ) / (WHHᵀ + eps)\n\n   Seeded ``numpy.random.default_rng`` init → deterministic for a given\n   ``(corpus, k, seed)``. Early-stops when the relative reconstruction error\n   ``‖X − WH‖ / ‖X‖`` changes by < ``tol`` between checks.\n5. **Topics**: each of the k rows of ``H`` is a topic; ``top_terms`` are the\n   highest-weight vocabulary entries; the label is the top-3 terms title-cased.\n6. **Doc→topic weights**: each row of ``W`` normalized to sum to 1. A grant's\n   *dominant* topic is its argmax.\n7. **Per-fiscal-year trend**: for each topic and FY - dominant-grant count,\n   summed weight, and *share* (topic weight ÷ all-topic weight that FY, so\n   growth is not inflated by the portfolio simply adding more grants per year).\n   ``growth_pct`` compares share across the trailing 2-FY window.\n8. **Funding anomalies** (separate routine, same module): z-score of\n   ``amount_usd`` within each ``program_area`` (population mean/std, groups of\n   >= ``min_group``); ``|z| >= 3.0`` → high severity, ``|z| >= 2.5`` → medium.\n\nDependency & Lambda-layer note\n------------------------------\nThe ONLY third-party dependency is **numpy** (see this function's\n``requirements.txt``): a single manylinux aarch64 wheel, ~40 MB unzipped:\nwell within Lambda's 250 MB unzipped budget next to the psycopg2 CommonLayer.\nscikit-learn was considered and rejected: sklearn + scipy would add ~170 MB\nand meaningfully slower cold starts for two routines (TF-IDF, NMF) that are\n~150 lines of transparent linear algebra here. The dense 400×2000 float64\nmatrix is ~6.4 MB; a full fit runs in well under a second at 1024 MB.\n\nThis module is pure computation - no AWS, no DB, no network - so it unit-tests\noffline (see tests/test_topic_model.py).\n\"\"\"\nfrom __future__ import annotations\n\nimport math\nimport re\nfrom dataclasses import dataclass, field\nfrom typing import Any, Dict, List, Optional, Sequence, Tuple\n\nimport numpy as np\n\nEPS = 1e-10\n\nTOKEN_RE = re.compile(r\"[a-z][a-z0-9\\-]{2,}\")\n\n# Standard English stopwords + the S&T boilerplate the templated abstracts\n# share across all program areas (leaving it in smears every topic together).\nSTOPWORDS = frozenset(\"\"\"\na about above after again against all also am an and any are as at be because\nbeen before being below between both but by can could did do does doing down\nduring each few for from further had has have having he her here hers him his\nhow i if in into is it its itself just me more most my no nor not now of off\non once only or other our ours out over own same she should so some such than\nthat the their theirs them then there these they this those through to too\nunder until up very was we were what when where which while who whom why will\nwith would you your yours\neffort efforts research researches project projects program programs approach\napproaches propose proposed proposes develop develops developing development\ndemonstrate demonstrates demonstrated demonstration novel new using based use\nused enable enables enabling result results study studies work provide\nprovides support supports key potential toward towards via within including\naddress addresses addressing improve improved improving advanced advance\ntechnical technology technologies capability capabilities system systems\nmethod methods application applications\n\"\"\".split())\n\n\n# --------------------------------------------------------------------------- #\n# Text -> TF-IDF matrix\n# --------------------------------------------------------------------------- #\ndef tokenize(text: str) -> List[str]:\n    \"\"\"Lowercase, extract word tokens (3+ chars), drop stopwords.\"\"\"\n    return [t for t in TOKEN_RE.findall((text or \"\").lower()) if t not in STOPWORDS]\n\n\ndef build_tfidf(\n    token_docs: Sequence[Sequence[str]],\n    *,\n    min_df: int = 3,\n    max_df_ratio: float = 0.5,\n    max_vocab: int = 2000,\n) -> Tuple[np.ndarray, List[str]]:\n    \"\"\"Build a dense L2-normalized TF-IDF matrix and its vocabulary.\n\n    Returns ``(X, vocab)`` where ``X`` is docs×terms float64. Raises\n    ``ValueError`` when no term survives the document-frequency filters.\n    \"\"\"\n    n_docs = len(token_docs)\n    df: Dict[str, int] = {}\n    tf_total: Dict[str, int] = {}\n    for tokens in token_docs:\n        seen = set()\n        for t in tokens:\n            tf_total[t] = tf_total.get(t, 0) + 1\n            if t not in seen:\n                seen.add(t)\n                df[t] = df.get(t, 0) + 1\n\n    max_df = max_df_ratio * n_docs\n    kept = [t for t, d in df.items() if d >= min_df and d <= max_df]\n    if not kept:\n        raise ValueError(\n            f\"no vocabulary survives min_df={min_df} / max_df_ratio={max_df_ratio} \"\n            f\"over {n_docs} documents\"\n        )"
    },
    {
      "file": "src/functions/document_ml/engine.py",
      "title": "Classifier training (readable Python)",
      "note": "The document model is auditable stdlib Python - no black box.",
      "startLine": 380,
      "endLine": 470,
      "totalLines": 625,
      "code": ") -> Tuple[List[TrainingSample], List[TrainingSample]]:\n    grouped: Dict[str, List[TrainingSample]] = defaultdict(list)\n    for sample in samples:\n        grouped[sample.label].append(sample)\n    train: List[TrainingSample] = []\n    test: List[TrainingSample] = []\n    for label in sorted(grouped):\n        ordered = sorted(\n            grouped[label],\n            key=lambda item: hashlib.sha256(\n                f\"{seed}:{item.sample_id}:{item.text}\".encode(\"utf-8\")\n            ).hexdigest(),\n        )\n        if len(ordered) < 2:\n            raise ValueError(f\"label {label!r} requires at least two samples\")\n        test.append(ordered[-1])\n        train.extend(ordered[:-1])\n    return train, test\n\n\ndef train_classifier(\n    samples: Sequence[TrainingSample], *, alpha: float = 1.0\n) -> Dict[str, Any]:\n    if alpha <= 0:\n        raise ValueError(\"alpha must be positive\")\n    observed_labels = {sample.label for sample in samples}\n    unknown_labels = observed_labels.difference(DOCUMENT_TAXONOMY)\n    if unknown_labels:\n        raise ValueError(\n            \"training contains unsupported labels: \" + \", \".join(sorted(unknown_labels))\n        )\n    labels = [label for label in DOCUMENT_TAXONOMY if label in observed_labels]\n    if len(labels) < 2:\n        raise ValueError(\"training requires at least two labels\")\n    class_docs: Counter[str] = Counter()\n    token_counts: Dict[str, Counter[str]] = {label: Counter() for label in labels}\n    vocabulary: set[str] = set()\n    digest_rows = []\n    for sample in sorted(\n        samples, key=lambda item: (item.label, item.sample_id, item.text)\n    ):\n        tokens = tokenize(sample.text)\n        if not tokens:\n            raise ValueError(\n                f\"training sample {sample.sample_id or '<unnamed>'} has no tokens\"\n            )\n        class_docs[sample.label] += 1\n        token_counts[sample.label].update(tokens)\n        vocabulary.update(tokens)\n        digest_rows.append(\n            {\"id\": sample.sample_id, \"label\": sample.label, \"text\": sample.text}\n        )\n    digest = hashlib.sha256(stable_json(digest_rows).encode(\"utf-8\")).hexdigest()\n    return {\n        \"contract\": MODEL_CONTRACT,\n        \"model_version\": f\"doc-nb-{digest[:12]}\",\n        \"algorithm\": \"multinomial-naive-bayes\",\n        \"taxonomy\": list(DOCUMENT_TAXONOMY),\n        \"training_split_seed\": TRAINING_SPLIT_SEED,\n        \"alpha\": float(alpha),\n        \"labels\": labels,\n        \"vocabulary\": sorted(vocabulary),\n        \"class_document_counts\": dict(class_docs),\n        \"token_counts\": {label: dict(token_counts[label]) for label in labels},\n        \"token_totals\": {label: sum(token_counts[label].values()) for label in labels},\n        \"training_document_count\": len(samples),\n        \"training_digest\": digest,\n    }\n\n\ndef predict(model: Mapping[str, Any], text: str) -> Dict[str, Any]:\n    tokens = tokenize(text)\n    labels = list(model[\"labels\"])\n    total_docs = sum(int(value) for value in model[\"class_document_counts\"].values())\n    vocabulary_size = max(1, len(model[\"vocabulary\"]))\n    alpha = float(model.get(\"alpha\", 1.0))\n    scores: Dict[str, float] = {}\n    for label in labels:\n        class_docs = int(model[\"class_document_counts\"][label])\n        score = math.log(class_docs / total_docs)\n        counts = model[\"token_counts\"][label]\n        denominator = float(model[\"token_totals\"][label]) + alpha * vocabulary_size\n        for token, count in Counter(tokens).items():\n            likelihood = (float(counts.get(token, 0)) + alpha) / denominator\n            score += count * math.log(likelihood)\n        scores[label] = score\n    best = max(scores, key=scores.get)\n    peak = max(scores.values())\n    exp_scores = {label: math.exp(value - peak) for label, value in scores.items()}\n    normalizer = sum(exp_scores.values()) or 1.0\n    probabilities = {label: exp_scores[label] / normalizer for label in labels}"
    },
    {
      "file": "src/functions/public_intelligence/model_execution.py",
      "title": "SageMaker provenance gates",
      "note": "Execution refuses unless every digest matches the pinned candidate.",
      "startLine": 320,
      "endLine": 470,
      "totalLines": 1390,
      "code": "    if not isinstance(value, dict):\n        raise ExecutionError(\"governed model execution evidence must be an object\")\n    etag = str(response.get(\"ETag\") or \"\").strip()\n    if not etag:\n        raise ExecutionError(\"governed model execution object has no conditional token\")\n    version = response.get(\"VersionId\")\n    return value, raw, str(version) if version else None, etag\n\n\ndef _pool() -> tuple[list[dict[str, Any]], dict[str, Any]]:\n    key = _required_env(\"PUBLIC_SBIR_CANDIDATE_POOL_KEY\")\n    value, raw, version = _read_json(key, max_bytes=MAX_POOL_BYTES)\n    expected_sha = _required_env(\"PUBLIC_SBIR_CANDIDATE_POOL_SHA256\")\n    if not SHA256_PATTERN.fullmatch(expected_sha) or _sha256(raw) != expected_sha:\n        raise ExecutionError(\"public SBIR candidate pool failed digest validation\")\n    boundary = value.get(\"dataBoundary\")\n    if value.get(\"contract\") != CANDIDATE_POOL_CONTRACT or not isinstance(boundary, dict):\n        raise ExecutionError(\"public SBIR candidate pool contract is invalid\")\n    if boundary != {\n        \"classification\": \"public\",\n        \"containsCui\": False,\n        \"labelsExcluded\": True,\n        \"piiMinimized\": True,\n    }:\n        raise ExecutionError(\"public SBIR candidate pool boundary is not permitted\")\n    source_dataset = value.get(\"sourceDataset\")\n    selection = value.get(\"selection\")\n    if (\n        not isinstance(source_dataset, dict)\n        or source_dataset.get(\"datasetId\")\n        != \"navy-sbir-current-phase-i-public-scoring\"\n        or not SHA256_PATTERN.fullmatch(str(source_dataset.get(\"sha256\") or \"\"))\n        or not isinstance(selection, dict)\n        or selection.get(\"cutoffExclusive\") != \"2023-12-31\"\n    ):\n        raise ExecutionError(\"public SBIR scoring cohort provenance is invalid\")\n    records = value.get(\"records\")\n    if not isinstance(records, list) or not records or len(records) > MAX_SAMPLE_SIZE_HARD:\n        raise ExecutionError(\"public SBIR candidate pool record count is invalid\")\n    cleaned: list[dict[str, Any]] = []\n    seen: set[str] = set()\n    for record in records:\n        if not isinstance(record, dict) or set(record) != ALLOWED_RECORD_KEYS:\n            raise ExecutionError(\"public SBIR candidate record contract is invalid\")\n        record_id = _clean_text(record.get(\"recordId\"), max_chars=120)\n        features = record.get(\"features\")\n        if not record_id or record_id in seen or not isinstance(features, dict):\n            raise ExecutionError(\"public SBIR candidate record identity is invalid\")\n        if set(features) != set(REQUIRED_FEATURES):\n            raise ExecutionError(\"public SBIR candidate feature contract is invalid\")\n        public_text = _clean_text(features.get(\"public_text\"), max_chars=10_000)\n        if not public_text or EMAIL_PATTERN.search(public_text):\n            raise ExecutionError(\"public SBIR candidate text failed minimization validation\")\n        try:\n            event_time = _parse_timestamp(record.get(\"eventTime\"))\n        except (TypeError, ValueError) as exc:\n            raise ExecutionError(\"public SBIR candidate event time is invalid\") from exc\n        if event_time.date().isoformat() <= str(selection[\"cutoffExclusive\"]):\n            raise ExecutionError(\"public SBIR scoring record overlaps the model evaluation cohort\")\n        normalized = {\n            \"recordId\": record_id,\n            \"eventTime\": _timestamp(event_time),\n            \"sourceRecordIds\": [\n                _clean_text(item, max_chars=120)\n                for item in record.get(\"sourceRecordIds\") or []\n                if _clean_text(item, max_chars=120)\n            ][:10],\n            \"features\": {\n                \"phase_i_amount_usd\": float(features[\"phase_i_amount_usd\"]),\n                \"title_character_count\": int(features[\"title_character_count\"]),\n                \"abstract_character_count\": int(features[\"abstract_character_count\"]),\n                \"abstract_token_count\": int(features[\"abstract_token_count\"]),\n                \"award_year\": int(features[\"award_year\"]),\n                \"topic_family\": _clean_text(features[\"topic_family\"], max_chars=80),\n                \"public_text\": public_text,\n            },\n        }\n        seen.add(record_id)\n        cleaned.append(normalized)\n    if int(selection.get(\"recordCount\") or 0) != len(cleaned):\n        raise ExecutionError(\"public SBIR scoring cohort record count is invalid\")\n    return cleaned, {\n        \"sha256\": expected_sha,\n        \"versionId\": version,\n        \"sourceDataset\": source_dataset,\n        \"selection\": selection,\n    }\n\n\ndef _model_package() -> dict[str, Any]:\n    package_arn = _required_env(\"PUBLIC_SBIR_MODEL_PACKAGE_ARN\")\n    expected_group_arn = _required_env(\"PUBLIC_SBIR_MODEL_PACKAGE_GROUP_ARN\")\n    expected_package_prefix = expected_group_arn.replace(\n        \"model-package-group/\", \"model-package/\"\n    ) + \"/\"\n    if not package_arn.startswith(expected_package_prefix):\n        raise ExecutionError(\"public SBIR model package is outside the configured registry\")\n    detail = _sagemaker_client().describe_model_package(ModelPackageName=package_arn)\n    approval = str(detail.get(\"ModelApprovalStatus\") or \"\")\n    if detail.get(\"ModelPackageStatus\") != \"Completed\" or approval != \"PendingManualApproval\":\n        raise ExecutionError(\"public SBIR model package is not a pending completed candidate\")\n    containers = (detail.get(\"InferenceSpecification\") or {}).get(\"Containers\") or []\n    if len(containers) != 1:\n        raise ExecutionError(\"public SBIR model package inference contract is invalid\")\n    container = containers[0]\n    model_data_url = str(container.get(\"ModelDataUrl\") or \"\")\n    parsed = urlparse(model_data_url)\n    model_data_key = parsed.path.lstrip(\"/\")\n    expected_model_data_key = _required_env(\"PUBLIC_SBIR_MODEL_DATA_KEY\")\n    if (\n        parsed.scheme != \"s3\"\n        or parsed.netloc != _bucket()\n        or model_data_key != expected_model_data_key\n    ):\n        raise ExecutionError(\"public SBIR model artifact is outside the governed boundary\")\n    image = str(container.get(\"Image\") or \"\")\n    image_digest = str(container.get(\"ImageDigest\") or \"\")\n    expected_image_digest = _required_env(\"PUBLIC_SBIR_IMAGE_DIGEST\")\n    environment = dict(container.get(\"Environment\") or {})\n    if (\n        not image\n        or not re.fullmatch(r\"sha256:[a-f0-9]{64}\", expected_image_digest)\n        or image_digest != expected_image_digest\n    ):\n        raise ExecutionError(\"public SBIR inference image is not digest-bound\")\n    if environment.get(\"SAGEMAKER_PROGRAM\") != \"inference.py\":\n        raise ExecutionError(\"public SBIR inference program is invalid\")\n    model_metrics = detail.get(\"ModelMetrics\") or {}\n    statistics = ((model_metrics.get(\"ModelQuality\") or {}).get(\"Statistics\") or {})\n    card_digest = str(statistics.get(\"ContentDigest\") or \"\").removeprefix(\"SHA256:\")\n    metadata = detail.get(\"CustomerMetadataProperties\") or {}\n    expected_training_arn = _required_env(\"PUBLIC_SBIR_TRAINING_JOB_ARN\")\n    expected_training_name = expected_training_arn.rsplit(\"/\", 1)[-1]\n    expected_artifact_sha = _required_env(\"PUBLIC_SBIR_MODEL_ARTIFACT_SHA256\")\n    expected_bundle_sha = _required_env(\"PUBLIC_SBIR_MODEL_BUNDLE_SHA256\")\n    expected_card_sha = _required_env(\"PUBLIC_SBIR_MODEL_CARD_SHA256\")\n    if (\n        not SHA256_PATTERN.fullmatch(expected_artifact_sha)\n        or not SHA256_PATTERN.fullmatch(expected_bundle_sha)\n        or not SHA256_PATTERN.fullmatch(expected_card_sha)\n        or card_digest != expected_card_sha\n        or metadata.get(\"training_job\") != expected_training_name\n        or metadata.get(\"artifact_sha256\") != expected_artifact_sha\n        or metadata.get(\"registry_bundle_sha256\") != expected_bundle_sha\n    ):\n        raise ExecutionError(\"public SBIR package provenance does not match the pinned candidate\")\n    expected_version = _required_env(\"PUBLIC_SBIR_MODEL_DATA_VERSION_ID\")\n    model_raw, actual_version = _read_object_version(\n        model_data_key,\n        max_bytes=MAX_MODEL_BYTES,\n        version_id=expected_version,"
    }
  ],
  "/analytics/": [
    {
      "file": "src/functions/analytics/topic_model.py",
      "title": "Topic model (TF-IDF + NMF)",
      "note": "Deterministic, numpy-only topic extraction over the corpus.",
      "startLine": 1,
      "endLine": 120,
      "totalLines": 439,
      "code": "\"\"\"Lightweight topic modeling over grant abstracts - numpy-only TF-IDF + NMF.\n\nAlgorithm (exact, so evaluators can check the math)\n---------------------------------------------------\n1. **Tokenize**: lowercase, extract ``[a-z][a-z0-9-]{2,}`` tokens, drop a\n   standard English stopword list plus S&T boilerplate (\"effort\", \"novel\",\n   \"demonstrate\", ...) that the templated abstracts share across every topic.\n2. **Vocabulary**: keep terms appearing in >= ``min_df`` documents and <=\n   ``max_df_ratio`` of documents (boilerplate guard), capped at ``max_vocab``\n   terms by total corpus frequency.\n3. **TF-IDF**: ``tfidf = tf * (ln((1+N)/(1+df)) + 1)`` (smoothed, sklearn-style\n   idf), then L2-normalize each document row.\n4. **NMF**: factor the docs×terms matrix ``X ≈ W·H`` (``W``: docs×k,\n   ``H``: k×terms, both non-negative) with Lee & Seung multiplicative updates\n   minimizing the Frobenius norm::\n\n       H <- H * (WᵀX) / (WᵀWH + eps)\n       W <- W * (XHᵀ) / (WHHᵀ + eps)\n\n   Seeded ``numpy.random.default_rng`` init → deterministic for a given\n   ``(corpus, k, seed)``. Early-stops when the relative reconstruction error\n   ``‖X − WH‖ / ‖X‖`` changes by < ``tol`` between checks.\n5. **Topics**: each of the k rows of ``H`` is a topic; ``top_terms`` are the\n   highest-weight vocabulary entries; the label is the top-3 terms title-cased.\n6. **Doc→topic weights**: each row of ``W`` normalized to sum to 1. A grant's\n   *dominant* topic is its argmax.\n7. **Per-fiscal-year trend**: for each topic and FY - dominant-grant count,\n   summed weight, and *share* (topic weight ÷ all-topic weight that FY, so\n   growth is not inflated by the portfolio simply adding more grants per year).\n   ``growth_pct`` compares share across the trailing 2-FY window.\n8. **Funding anomalies** (separate routine, same module): z-score of\n   ``amount_usd`` within each ``program_area`` (population mean/std, groups of\n   >= ``min_group``); ``|z| >= 3.0`` → high severity, ``|z| >= 2.5`` → medium.\n\nDependency & Lambda-layer note\n------------------------------\nThe ONLY third-party dependency is **numpy** (see this function's\n``requirements.txt``): a single manylinux aarch64 wheel, ~40 MB unzipped:\nwell within Lambda's 250 MB unzipped budget next to the psycopg2 CommonLayer.\nscikit-learn was considered and rejected: sklearn + scipy would add ~170 MB\nand meaningfully slower cold starts for two routines (TF-IDF, NMF) that are\n~150 lines of transparent linear algebra here. The dense 400×2000 float64\nmatrix is ~6.4 MB; a full fit runs in well under a second at 1024 MB.\n\nThis module is pure computation - no AWS, no DB, no network - so it unit-tests\noffline (see tests/test_topic_model.py).\n\"\"\"\nfrom __future__ import annotations\n\nimport math\nimport re\nfrom dataclasses import dataclass, field\nfrom typing import Any, Dict, List, Optional, Sequence, Tuple\n\nimport numpy as np\n\nEPS = 1e-10\n\nTOKEN_RE = re.compile(r\"[a-z][a-z0-9\\-]{2,}\")\n\n# Standard English stopwords + the S&T boilerplate the templated abstracts\n# share across all program areas (leaving it in smears every topic together).\nSTOPWORDS = frozenset(\"\"\"\na about above after again against all also am an and any are as at be because\nbeen before being below between both but by can could did do does doing down\nduring each few for from further had has have having he her here hers him his\nhow i if in into is it its itself just me more most my no nor not now of off\non once only or other our ours out over own same she should so some such than\nthat the their theirs them then there these they this those through to too\nunder until up very was we were what when where which while who whom why will\nwith would you your yours\neffort efforts research researches project projects program programs approach\napproaches propose proposed proposes develop develops developing development\ndemonstrate demonstrates demonstrated demonstration novel new using based use\nused enable enables enabling result results study studies work provide\nprovides support supports key potential toward towards via within including\naddress addresses addressing improve improved improving advanced advance\ntechnical technology technologies capability capabilities system systems\nmethod methods application applications\n\"\"\".split())\n\n\n# --------------------------------------------------------------------------- #\n# Text -> TF-IDF matrix\n# --------------------------------------------------------------------------- #\ndef tokenize(text: str) -> List[str]:\n    \"\"\"Lowercase, extract word tokens (3+ chars), drop stopwords.\"\"\"\n    return [t for t in TOKEN_RE.findall((text or \"\").lower()) if t not in STOPWORDS]\n\n\ndef build_tfidf(\n    token_docs: Sequence[Sequence[str]],\n    *,\n    min_df: int = 3,\n    max_df_ratio: float = 0.5,\n    max_vocab: int = 2000,\n) -> Tuple[np.ndarray, List[str]]:\n    \"\"\"Build a dense L2-normalized TF-IDF matrix and its vocabulary.\n\n    Returns ``(X, vocab)`` where ``X`` is docs×terms float64. Raises\n    ``ValueError`` when no term survives the document-frequency filters.\n    \"\"\"\n    n_docs = len(token_docs)\n    df: Dict[str, int] = {}\n    tf_total: Dict[str, int] = {}\n    for tokens in token_docs:\n        seen = set()\n        for t in tokens:\n            tf_total[t] = tf_total.get(t, 0) + 1\n            if t not in seen:\n                seen.add(t)\n                df[t] = df.get(t, 0) + 1\n\n    max_df = max_df_ratio * n_docs\n    kept = [t for t, d in df.items() if d >= min_df and d <= max_df]\n    if not kept:\n        raise ValueError(\n            f\"no vocabulary survives min_df={min_df} / max_df_ratio={max_df_ratio} \"\n            f\"over {n_docs} documents\"\n        )"
    }
  ],
  "/dashboard/": [
    {
      "file": "src/functions/dashboard/app.py",
      "title": "Role-scoped dashboard projection",
      "note": "KPIs computed server-side from the same receipts, per caller scope.",
      "startLine": 1,
      "endLine": 120,
      "totalLines": 579,
      "code": "\"\"\"Server-backed executive dashboard projection.\n\nGET /dashboard returns the portfolio metrics used by the decision workspace.\nThe endpoint accepts four optional filters: program_area, fiscal_year,\norg_unit, and q. Request values are never interpolated into SQL. PostgreSQL\nrow-level security remains the source of truth for row scope and column-level\nsecurity remains the source of truth for funding visibility.\n\nFilter options are calculated before request filters are applied, but inside\nthe caller's RLS transaction. A viewer therefore never receives an option for\nan organization they cannot read. Corporate callers use the GUC-gated\ngrants_curated_corp view for funding values. Every other caller reads only the\nmasked base table.\n\"\"\"\nfrom __future__ import annotations\n\nfrom dataclasses import dataclass\nimport hashlib\nimport json\nimport os\nimport re\nimport time\nfrom typing import Any, Dict, List, Optional, Tuple\n\nfrom compass_common import db, http\n\n\nCACHE: Dict[str, Dict[str, Any]] = {}\nCACHE_TTL_SECONDS = max(0, int(os.environ.get(\"DASHBOARD_CACHE_TTL_SECONDS\", \"1\")))\n\n_BASE_TABLE = \"grants_curated\"\n_CORP_VIEW = \"grants_curated_corp\"\n_ALLOWED_QUERY_KEYS = frozenset({\"program_area\", \"fiscal_year\", \"org_unit\", \"q\"})\n_SAFE_YEAR = re.compile(r\"^[0-9]{4}$\")\n_MAX_FILTER_LENGTH = 120\n_MAX_SEARCH_LENGTH = 160\n\n\n@dataclass(frozen=True)\nclass DashboardFilters:\n    program_area: Optional[str] = None\n    fiscal_year: Optional[int] = None\n    org_unit: Optional[str] = None\n    q: Optional[str] = None\n\n    @property\n    def has_portfolio_filter(self) -> bool:\n        return any(\n            value is not None\n            for value in (self.program_area, self.fiscal_year, self.org_unit, self.q)\n        )\n\n    def as_response(self) -> Dict[str, Any]:\n        return {\n            \"program_area\": self.program_area,\n            \"fiscal_year\": self.fiscal_year,\n            \"org_unit\": self.org_unit,\n            \"q\": self.q,\n        }\n\n\ndef _clean_text(value: Any, *, field: str, max_length: int) -> Optional[str]:\n    if value is None:\n        return None\n    cleaned = str(value).strip()\n    if not cleaned:\n        return None\n    if len(cleaned) > max_length:\n        raise ValueError(f\"{field} exceeds {max_length} characters\")\n    if any(ord(char) < 32 for char in cleaned):\n        raise ValueError(f\"{field} contains unsupported control characters\")\n    return cleaned\n\n\ndef _parse_filters(params: Dict[str, Any]) -> DashboardFilters:\n    unknown = sorted(set(params) - _ALLOWED_QUERY_KEYS)\n    if unknown:\n        raise ValueError(f\"unsupported dashboard filter: {unknown[0]}\")\n\n    year_raw = _clean_text(\n        params.get(\"fiscal_year\"),\n        field=\"fiscal_year\",\n        max_length=4,\n    )\n    fiscal_year: Optional[int] = None\n    if year_raw is not None:\n        if not _SAFE_YEAR.fullmatch(year_raw):\n            raise ValueError(\"fiscal_year must be a four-digit year\")\n        fiscal_year = int(year_raw)\n        if fiscal_year < 1900 or fiscal_year > 2200:\n            raise ValueError(\"fiscal_year is outside the supported range\")\n\n    return DashboardFilters(\n        program_area=_clean_text(\n            params.get(\"program_area\"),\n            field=\"program_area\",\n            max_length=_MAX_FILTER_LENGTH,\n        ),\n        fiscal_year=fiscal_year,\n        org_unit=_clean_text(\n            params.get(\"org_unit\"),\n            field=\"org_unit\",\n            max_length=_MAX_FILTER_LENGTH,\n        ),\n        q=_clean_text(\n            params.get(\"q\"),\n            field=\"q\",\n            max_length=_MAX_SEARCH_LENGTH,\n        ),\n    )\n\n\ndef _escape_like(value: str) -> str:\n    \"\"\"Treat percent, underscore, and backslash as search text, not patterns.\"\"\"\n    return value.replace(\"\\\\\", \"\\\\\\\\\").replace(\"%\", \"\\\\%\").replace(\"_\", \"\\\\_\")\n\n\ndef _grant_predicates(\n    filters: DashboardFilters,\n    *,"
    }
  ],
  "/export/": [
    {
      "file": "src/functions/export/app.py",
      "title": "Approval fingerprint + single-use consume",
      "note": "The fingerprint that binds an approval to one exact query, and the FOR UPDATE consume that makes tokens single-use.",
      "startLine": 250,
      "endLine": 430,
      "totalLines": 967,
      "code": "            raise FilterError(\n                \"column-level security: amount_usd is not readable by this role\",\n                status=403,\n                column=name,\n            )\n        if name not in cols:\n            cols.append(name)\n    if not cols:\n        raise FilterError(\"columns must not be empty\", status=400)\n    return cols\n\n\n# --------------------------------------------------------------------------- #\n# Approval tokens\n# --------------------------------------------------------------------------- #\ndef filter_fingerprint(org_unit: str, fmt: str, columns: Sequence[str], applied: Dict[str, Any]) -> str:\n    \"\"\"Deterministic id for *this exact* export request.\n\n    Returned in the 428 body as ``subject_id`` so an approval is granted for a\n    specific query, from a specific org, in a specific shape, not a standing\n    licence to export anything. Includes ``org_unit`` so an approval issued to\n    one org cannot be replayed by another.\n    \"\"\"\n    canonical = json.dumps(\n        {\n            \"org_unit\": org_unit,\n            \"format\": fmt,\n            \"columns\": sorted(columns),\n            \"filters\": {k: str(v) for k, v in sorted(applied.items())},\n        },\n        sort_keys=True,\n        separators=(\",\", \":\"),\n    )\n    return \"exp-\" + hashlib.sha256(canonical.encode(\"utf-8\")).hexdigest()[:16]\n\n\nAPPROVAL_TOKEN_PATTERN = re.compile(\n    r\"^apr-(?P<approval_id>[1-9][0-9]*)\\.(?P<secret>[A-Za-z0-9_-]{43})$\"\n)\n\n\ndef parse_token(token: str) -> Optional[Tuple[int, str]]:\n    \"\"\"Parse the opaque token shape and reject legacy id-only tokens.\"\"\"\n    matched = APPROVAL_TOKEN_PATTERN.fullmatch((token or \"\").strip())\n    if matched is None:\n        return None\n    return int(matched.group(\"approval_id\")), matched.group(\"secret\")\n\n\ndef capability_digest(secret: str) -> str:\n    \"\"\"Return the SHA-256 digest stored by the approval service.\"\"\"\n    return hashlib.sha256(secret.encode(\"utf-8\")).hexdigest()\n\n\ndef check_approval(\n    conn,\n    token: str,\n    subject_id: str,\n    *,\n    actor: str = \"unknown\",\n) -> Tuple[bool, Dict[str, Any]]:\n    \"\"\"Atomically validate and consume an export approval token.\n\n    The approval must be approved, unexpired, unused, and bound to the exact\n    export fingerprint. ``FOR UPDATE`` plus the conditional consume update\n    means two concurrent exports cannot spend the same approval.\n    \"\"\"\n    parsed = parse_token(token)\n    if parsed is None:\n        return False, {\n            \"code\": \"approval_capability_invalid\",\n            \"reason\": \"malformed or legacy approval_token\",\n        }\n    approval_id, presented_secret = parsed\n    presented_hash = capability_digest(presented_secret)\n\n    with conn.cursor() as cur:\n        cur.execute(\n            \"\"\"\n            SELECT id, subject_type, subject_id, state, requested_by, decided_by,\n                   decided_at, expires_at, consumed_at, consumed_by,\n                   capability_hash,\n                   (expires_at IS NOT NULL AND expires_at > now()) AS is_active\n              FROM approvals\n             WHERE id = %s\n             FOR UPDATE\n            \"\"\",\n            (approval_id,),\n        )\n        row = cur.fetchone()\n\n    if row is None:\n        return False, {\n            \"code\": \"approval_capability_invalid\",\n            \"reason\": f\"approval {approval_id} does not exist\",\n            \"approval_id\": approval_id,\n        }\n\n    stored_hash = row[10]\n    if not isinstance(stored_hash, str) or not hmac.compare_digest(\n        stored_hash,\n        presented_hash,\n    ):\n        return False, {\n            \"code\": \"approval_capability_invalid\",\n            \"reason\": \"approval_token secret rejected\",\n            \"approval_id\": approval_id,\n        }\n\n    detail = {\n        \"approval_id\": int(row[0]),\n        \"subject_type\": row[1],\n        \"subject_id\": row[2],\n        \"state\": row[3],\n        \"requested_by\": row[4],\n        \"decided_by\": row[5],\n        \"decided_at\": row[6].isoformat() if isinstance(row[6], (datetime, date)) else row[6],\n        \"expires_at\": row[7].isoformat() if isinstance(row[7], (datetime, date)) else row[7],\n        \"consumed_at\": row[8].isoformat() if isinstance(row[8], (datetime, date)) else row[8],\n        \"consumed_by\": row[9],\n    }\n\n    if detail[\"state\"] != \"approved\":\n        detail[\"code\"] = \"approval_capability_invalid\"\n        detail[\"reason\"] = f\"approval {approval_id} is '{detail['state']}', not 'approved'\"\n        return False, detail\n    if (detail[\"subject_type\"] or \"\").lower() != \"export\":\n        detail[\"code\"] = \"approval_capability_invalid\"\n        detail[\"reason\"] = (\n            f\"approval {approval_id} was raised for subject_type \"\n            f\"'{detail['subject_type']}', not 'export'\"\n        )\n        return False, detail\n\n    subject = detail[\"subject_id\"] or \"\"\n    if not subject_id.startswith(\"exp-\") or subject != subject_id:\n        detail[\"code\"] = \"approval_capability_unbound\"\n        detail[\"binding\"] = \"rejected\"\n        detail[\"reason\"] = (\n            \"approval is not bound to this exact export fingerprint \"\n            f\"(approved {subject or 'empty'}; expected {subject_id})\"\n        )\n        return False, detail\n    if detail[\"consumed_at\"] is not None:\n        detail[\"code\"] = \"approval_capability_consumed\"\n        detail[\"reason\"] = (\n            f\"approval {approval_id} was already consumed at \"\n            f\"{detail['consumed_at']}\"\n        )\n        return False, detail\n    if not bool(row[11]):\n        detail[\"code\"] = \"approval_capability_expired\"\n        detail[\"reason\"] = (\n            f\"approval {approval_id} expired at \"\n            f\"{detail['expires_at'] or 'an unspecified time'}\"\n        )\n        return False, detail\n\n    with conn.cursor() as cur:\n        cur.execute(\n            \"\"\"\n            UPDATE approvals\n               SET consumed_at = now(), consumed_by = %s\n             WHERE id = %s\n               AND state = 'approved'\n               AND consumed_at IS NULL\n               AND expires_at > now()\n               AND capability_hash = %s\n            RETURNING consumed_at\n            \"\"\",\n            (actor, approval_id, presented_hash),\n        )\n        consumed = cur.fetchone()\n    if consumed is None:\n        detail[\"code\"] = \"approval_capability_unavailable\"\n        detail[\"reason\"] = (\n            f\"approval {approval_id} could not be consumed because it expired \"\n            \"or was used by another request\"\n        )\n        return False, detail\n"
    },
    {
      "file": "src/functions/export/app.py",
      "title": "HTTP 428 guard + governed release",
      "note": "The server-side row count, the 428 'approval required' answer, and the release path that writes the audit receipt.",
      "startLine": 640,
      "endLine": 790,
      "totalLines": 967,
      "code": "# POST /export\n# --------------------------------------------------------------------------- #\ndef run_export(\n    conn, body: Dict[str, Any], *, actor: str, role: str, org_unit: str\n) -> Dict[str, Any]:\n    \"\"\"Execute the guarded export. Returns ``{\"status\", \"body\", \"payload\"?}``.\n\n    ``payload`` (bytes + content type) is returned separately so the caller can\n    deliver it *after* the read transaction commits. The audit row lands first.\n    \"\"\"\n    fmt = str(body.get(\"format\") or \"csv\").strip().lower()\n    if fmt not in FORMATS:\n        return {\"status\": 400, \"body\": {\"error\": f\"format must be one of {list(FORMATS)}\", \"format\": fmt}}\n\n    amount_visible, mask_reason = amount_readable(conn, role)\n    filters = body.get(\"filters\") or {}\n    if not isinstance(filters, dict):\n        return {\"status\": 400, \"body\": {\"error\": \"filters must be an object\"}}\n\n    try:\n        columns = resolve_columns(body.get(\"columns\"), amount_visible)\n        where, params, applied = build_where(filters, amount_visible)\n        limit = parse_limit(filters)\n    except FilterError as exc:\n        return {\"status\": exc.status, \"body\": {\"error\": str(exc), **exc.extra}}\n\n    amount_needed = AMOUNT_COLUMN in columns or any(k in AMOUNT_FILTER_SPEC for k in applied)\n    relation = _relation(amount_needed and amount_visible)\n    masked_fields: List[str] = [] if amount_visible else [AMOUNT_COLUMN]\n    request_filters = dict(applied)\n    if limit is not None:\n        request_filters[\"limit\"] = limit\n\n    matched = count_rows(conn, relation, where, params)\n    subject_id = filter_fingerprint(org_unit, fmt, columns, request_filters)\n    max_rows = config.export_max_rows()\n\n    base_detail = {\n        \"role\": role,\n        \"org_unit\": org_unit,\n        \"format\": fmt,\n        \"columns\": columns,\n        \"filters\": request_filters,\n        \"matched_rows\": matched,\n        \"max_rows\": max_rows,\n        \"subject_id\": subject_id,\n    }\n\n    # The hard materialization cap cannot be overridden by an approval. Check\n    # it before consuming a one-time token so a request that can never run does\n    # not spend the approval.\n    to_write = matched if limit is None else min(matched, limit)\n    if to_write > HARD_MAX_ROWS:\n        audit.write_audit(\n            conn,\n            actor=actor,\n            action=\"export_denied\",\n            resource=\"grants_curated\",\n            detail={**base_detail, \"decision\": \"denied\", \"reason\": \"hard row cap\"},\n        )\n        return {\n            \"status\": 413,\n            \"body\": {\n                \"error\": f\"export of {to_write} rows exceeds the {HARD_MAX_ROWS}-row \"\n                         \"materialisation cap for this function\",\n                \"row_count\": to_write,\n                \"hard_max_rows\": HARD_MAX_ROWS,\n            },\n        }\n\n    # --- aggregation guard --------------------------------------------------- #\n    token = (body.get(\"approval_token\") or \"\").strip()\n    approval_detail: Optional[Dict[str, Any]] = None\n    if matched > max_rows:\n        if not token:\n            audit.write_audit(\n                conn,\n                actor=actor,\n                action=\"export_blocked\",\n                resource=\"grants_curated\",\n                detail={**base_detail, \"decision\": \"blocked\", \"reason\": \"aggregation guard\"},\n            )\n            return {\n                \"status\": 428,\n                \"body\": {\n                    \"error\": \"approval_required\",\n                    \"row_count\": matched,\n                    \"max_rows\": max_rows,\n                    \"subject_type\": \"export\",\n                    \"subject_id\": subject_id,\n                    \"how_to_clear\": (\n                        'POST /approvals {\"subject_type\":\"export\",\"subject_id\":'\n                        f'\"{subject_id}\",\"action\":\"request\"}}, have a poweruser approve it, '\n                        \"then retry this export with the returned approval_token.\"\n                    ),\n                },\n            }\n\n        ok, approval_detail = check_approval(\n            conn,\n            token,\n            subject_id,\n            actor=actor,\n        )\n        if not ok:\n            audit.write_audit(\n                conn,\n                actor=actor,\n                action=\"export_denied\",\n                resource=\"grants_curated\",\n                detail={**base_detail, \"decision\": \"denied\", \"approval\": approval_detail},\n            )\n            return {\n                \"status\": 403,\n                \"body\": {\n                    \"error\": \"approval_token rejected\",\n                    \"code\": approval_detail.get(\n                        \"code\", \"approval_capability_invalid\"\n                    ),\n                    \"reason\": (\n                        \"opaque approval capability is invalid, expired, consumed, \"\n                        \"or not bound to this request\"\n                    ),\n                    \"row_count\": matched,\n                    \"max_rows\": max_rows,\n                    \"subject_type\": \"export\",\n                    \"subject_id\": subject_id,\n                },\n            }\n\n    rows = fetch_rows(conn, relation, columns, where, params, limit)\n\n    # --- serialise ------------------------------------------------------------ #\n    export_id = f\"export-{datetime.now(timezone.utc):%Y%m%dT%H%M%SZ}-{uuid.uuid4().hex[:8]}\"\n    effective_format = fmt\n    note: Optional[str] = None\n\n    if fmt == \"parquet\" and not parquet_available():\n        effective_format = \"csv\"\n        note = PARQUET_MISSING_NOTE\n\n    meta = {\n        \"export_id\": export_id,\n        \"generated_at\": datetime.now(timezone.utc).isoformat(),\n        \"source\": \"compass.grants_curated\",\n        \"org_unit\": org_unit,\n        \"role\": role,\n        \"columns\": list(columns),\n        \"filters\": request_filters,\n        \"matched_rows\": matched,\n        \"row_count\": len(rows),"
    }
  ]
};
